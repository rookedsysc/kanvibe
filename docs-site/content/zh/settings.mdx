---
title: 设置
---

# 设置

KanVibe 设置页把项目注册、显示偏好、终端 pane 配置、通知、任务创建默认值和后台 Sync 集中在一个地方管理。设置会保存到应用数据库中，其中多项会在保存后立即反映到看板或任务详情工作区。

<figure className="kv-shot">
  <img src="/screenshots/settings.png" alt="KanVibe 设置页面" />
  <figcaption>在设置页管理外观、Pane 布局、详情页默认值、通知状态、后台 Sync 和项目注册。</figcaption>
</figure>

## 设置页可管理的内容

<div className="kv-grid">
  <div className="kv-card">
    <h3>外观</h3>
    <p>选择系统、深色或浅色模式，统一整个应用的显示方式。</p>
  </div>
  <div className="kv-card">
    <h3>Pane 布局</h3>
    <p>把终端 pane 布局和启动命令保存为全局默认值，也可以为单个项目设置 override。</p>
  </div>
  <div className="kv-card">
    <h3>详情页</h3>
    <p>决定打开任务工作区时左侧边栏是否默认折叠。</p>
  </div>
  <div className="kv-card">
    <h3>任务创建</h3>
    <p>为新任务选择默认会话类型：tmux 或 zellij。</p>
  </div>
  <div className="kv-card">
    <h3>通知</h3>
    <p>开启浏览器通知，并选择 PROGRESS、PENDING、REVIEW 中哪些状态变化需要提醒。</p>
  </div>
  <div className="kv-card">
    <h3>后台 Sync</h3>
    <p>按计划同步项目 worktree 与 PR 状态，并在 1 到 1440 分钟之间调整执行间隔。</p>
  </div>
</div>

## 外观

| 设置项 | 选项 | 行为 |
| --- | --- | --- |
| 主题 | 系统、深色、浅色 | 改变整个应用的颜色模式。系统会跟随 macOS 显示模式，深色和浅色会把应用固定为对应模式并立即生效。 |

主题保存后会立刻应用。看板、任务详情和下一次应用启动都会复用同一偏好。

## Pane 布局

设置页中的 **面板布局设置** 链接会打开 tmux pane 分割和启动命令编辑器。

| 设置项 | 说明 |
| --- | --- |
| 全局默认值 | 项目没有专属设置时，所有新终端会话使用的默认布局。 |
| 项目专属设置 | 为单个项目覆盖布局和命令。没有 override 的项目继续使用全局默认值。 |
| 布局类型 | 可选择 `单面板`、`上下分割`、`左右分割`、`左 + 右上下`、`左上下 + 右`、`四分割`。 |
| Pane 启动命令 | 为每个 pane 位置输入启动命令。例如一个 pane 运行 `pnpm dev`，另一个 pane 运行测试或日志命令。 |
| 保存 / 重置 | 保存后会应用到未来创建的会话。项目专属设置可通过 **重置为全局默认值** 删除 override。 |

Pane 布局能让任务终端保持可重复。如果每个 worktree 都需要服务器、测试和日志窗口，可以把这些命令保存一次，新任务工作区就会以相同结构打开。

## 详情页

| 设置项 | 默认值 | 行为 |
| --- | --- | --- |
| 默认折叠侧栏 | 关闭 | 控制进入任务详情页时左侧信息面板是否默认折叠。想给终端留出更多空间时可开启；想一直看到标题、项目、优先级和会话信息时保持关闭。 |

该设置只改变任务详情工作区的打开方式，不会修改任务数据或终端会话。

## 任务创建

| 设置项 | 选项 | 行为 |
| --- | --- | --- |
| 默认会话类型 | `tmux`、`zellij` | 创建新任务时默认选择的终端后端。tmux 是默认值。使用 zellij 时，本机或远程目标环境需要安装 zellij。 |

会话类型设置只改变新建任务的默认选项，不会批量改写已有任务。

## 通知

| 设置项 | 选项 | 行为 |
| --- | --- | --- |
| 浏览器通知 | 开启 / 关闭 | 控制任务状态变化、PR 事件和后台 Sync 检查项是否可以显示系统通知。 |
| 通知状态 | PROGRESS、PENDING、REVIEW | 仅当任务变更为所选状态时发送通知。默认包含 PROGRESS、PENDING、REVIEW。TODO 和 DONE 通常由用户主动管理，因此不在状态过滤器中。 |

通知中心和浏览器通知会提示 AI 代理状态变化、PR 事件和后台 Sync 事件。点击已关联任务的通知，会直接打开该任务的详情工作区。缩小状态范围后，可以只保留 PENDING 提问或 REVIEW 待检查等真正需要关注的事件。

## 后台 Sync

| 设置项 | 范围 | 行为 |
| --- | --- | --- |
| 后台 Sync | 开启 / 关闭 | 应用打开时，定期检查项目 worktree、PR 状态和任务 pull 结果。 |
| 同步间隔 | 1–1440 分钟 | 后台 Sync 的执行频率。默认值为 10 分钟，以分钟保存。无效值不会保存，并会恢复到上一个有效值。 |

后台 Sync 会收集合并完成的 PR、新发现的 worktree、pull 成功或失败、PR 查询失败等检查项。即使某个同步阶段失败，其余阶段仍会继续执行，失败原因会显示在后台 Sync 检查通知中。

## 目录扫描

| 设置项 | 说明 |
| --- | --- |
| SSH 主机选择 | 如果存在 SSH 主机，可在扫描前选择本地或指定远程主机。 |
| 要扫描的目录 | 使用文件夹搜索输入选择本地或远程目录。文件夹搜索支持方向键移动、Tab 进入子文件夹、Enter 选择、Backspace 返回上级、Esc 关闭。 |
| 扫描并注册 | 在所选目录下查找 git 仓库并注册为项目。已注册仓库会被跳过，新发现的 worktree 会注册为 TODO 任务。 |
| 扫描结果 | 显示已注册、已跳过和错误数量。没有新项目或未找到 git 仓库时，会直接显示对应消息。 |

## 已注册项目

项目列表会显示每个项目的名称、repo path、SSH 主机和默认分支。删除项目只会从数据库中移除 KanVibe 项目和任务记录，磁盘上的 git branch、worktree 和文件都会保留。只有在不再希望 KanVibe 跟踪该项目时才删除。
