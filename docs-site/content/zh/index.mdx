---
title: KanVibe 文档
---

<div className="kv-hero">
  <span className="kv-eyebrow">KanVibe Docs · Nextra 4</span>
  <h1>把 AI 代理工作整合进看板、终端和分支流程。</h1>
  <div className="kv-hero-copy">
    KanVibe 可把 Claude Code、Gemini CLI、Codex CLI、OpenCode 的任务连接到真实的 git worktree 与 tmux/zellij 会话。每个任务都有自己的终端界面，可在同一流程中自由切换 AI 代理执行、手动命令、构建/测试与日志查看。
  </div>
  <div className="kv-actions">
    <a className="kv-button kv-button-primary" href="/zh/installation">开始安装</a>
    <a className="kv-button" href="/zh/features">查看功能</a>
  </div>
  <div className="kv-chip-row">
    <span className="kv-chip">한국어 · English · 中文</span>
    <span className="kv-chip">Next.js 16 + Nextra 4</span>
    <span className="kv-chip">Keyboard-first</span>
  </div>
</div>

<figure className="kv-shot">
  <img src="/screenshots/board.png" alt="KanVibe 看板界面" />
  <figcaption>KanVibe 主看板集中展示项目颜色、优先级、PR 徽章和终端会话标签。</figcaption>
</figure>

<figure className="kv-shot">
  <img src="/screenshots/terminal-workspace.png" alt="每个任务的终端工作区" />
  <figcaption>每个任务都会打开独立终端工作区，让 AI 代理执行与直接 shell 命令同样容易访问。</figcaption>
</figure>

<div className="kv-grid">
  <div className="kv-card">
    <h3>基于分支的任务管理</h3>
    <p>创建任务时，KanVibe 会把 branch、worktree 和 tmux/zellij 会话绑定成一个工作单元。</p>
  </div>
  <div className="kv-card">
    <h3>AI 代理 Hooks</h3>
    <p>Claude、Gemini、Codex、OpenCode 事件会自动推动任务进入 PROGRESS、PENDING、REVIEW。</p>
  </div>
  <div className="kv-card">
    <h3>任务级 AI 聊天历史</h3>
    <p>在任务中加载 Claude、Gemini、Codex、OpenCode 对话，并通过 provider OR 过滤、正文搜索和 role 过滤找到所需上下文。</p>
  </div>
  <div className="kv-card">
    <h3>任务专属终端界面</h3>
    <p>每个任务都有自己的 tmux/zellij 会话，可自由交给 AI 代理处理，也可以随时直接运行命令。</p>
  </div>
  <div className="kv-card">
    <h3>键盘优先 UI</h3>
    <p>用快捷键打开项目过滤、快速搜索、通知和详情面板，同时保持终端焦点可预测。</p>
  </div>
  <div className="kv-card">
    <h3>新 Worktree 自动检测</h3>
    <p>后台 Sync 定期扫描项目，发现外部创建的 git worktree 后自动注册为任务并安装 hook。</p>
  </div>
  <div className="kv-card">
    <h3>Merged PR 自动检测</h3>
    <p>通过 GitHub CLI 定期检查 PR 状态，发现已合并 PR 后发送通知并引导转为 DONE。</p>
  </div>
  <div className="kv-card">
    <h3>通知中心</h3>
    <p>通过 Cmd+Shift+I 打开通知中心，查看 AI 代理状态变更、PR 合并等事件，并直接跳转到相关任务。</p>
  </div>
  <div className="kv-card">
    <h3>会话自动关联</h3>
    <p>后台 Sync 检测到运行中的 tmux/zellij 会话时，自动关联到对应任务并将状态转为 PROGRESS。</p>
  </div>
  <div className="kv-card">
    <h3>DONE 自动清理</h3>
    <p>将任务移到 DONE 或删除时，自动在后台清理 git worktree、branch 和 tmux/zellij 会话。项目根分支受保护。</p>
  </div>
</div>
