Codebase uses TypeScript with path alias `@/`. Tests use Vitest and often follow Given/When/Then comments. Files prefer double quotes and semicolons. React client hooks/components are in src/hooks and src/components; server/app routes under src/app. Comments are sparse and explanatory rather than verbose. Existing code favors small exported helpers/functions instead of heavy abstraction.