appId: com.kanvibe.desktop
productName: KanVibe
asar: true
asarUnpack:
  - node_modules/better-sqlite3/**
npmRebuild: false

directories:
  output: dist
  buildResources: resources

files:
  - build/renderer/**/*
  - build/main/**/*
  - electron/**/*
  - messages/**/*
  - package.json
  - public/**/*
  - tsconfig.json

extraResources:
  - from: resources/database
    to: database
    filter:
      - app.seed.db

mac:
  icon: resources/icon.icns
  minimumSystemVersion: "12.0.0"
  target:
    - dmg

dmg:
  icon: resources/icon.icns
  artifactName: "KanVibe-${version}.${ext}"

publish: null
